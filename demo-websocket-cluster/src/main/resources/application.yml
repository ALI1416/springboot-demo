spring:
  devtools:
    restart:
      enabled: true
  servlet:
    multipart:
      max-file-size: 8MB
  data:
    redis:
      database: 0
      host: 127.0.0.1
      port: 6379
      password:
      timeout: 10s

#server:
#  port: 8081

logging:
  level:
    root: info
  file:
    name: /log/demo-websocket-cluster.log
