spring:
  devtools:
    restart:
      enabled: true
  servlet:
    multipart:
      max-file-size: 8MB

logging:
  level:
    root: info
  file:
    name: /log/demo-config.log

# 测试
test:
  stringType: abcde
  intType: 12345
