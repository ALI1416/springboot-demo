spring:
  devtools:
    restart:
      enabled: true
  servlet:
    multipart:
      max-file-size: 8MB

minio:
  uri: http://127.0.0.1:9000
  username: minioadmin
  password: minioadmin

logging:
  level:
    root: info
  file:
    name: /log/demo-minio.log
